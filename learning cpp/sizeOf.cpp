#include <iostream>
using namespace std;

int main(){

    //sizeOf is used to get the size of a data type in bytes
    //syntax: sizeof(data_type)

    int num = 10000;
    cout << sizeof(num) << " bytes\n"; 

    return 0;
}